pyyaml = "==5.4.1"  # Use an older version known to work without PEP 517 issues


python = "^3.12"
transformers = "==4.29.0"
datasets = "==2.5.0"
torch = "==2.3.1"
scikit-learn = "==1.0.1"
fasttext = "==0.9.2"
nlpaug = "==1.1.10"
flask = "==2.0.2"
pandas = "==1.3.4"
numpy = ">=1.24.0"  # Specify a version of numpy that supports Python 3.12
pyyaml = "==6.0.1"  # Change this to a newer version